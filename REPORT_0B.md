# Playable Slice 0B — Actually Playable 交付报告

日期：2026-08-16 · 基线：Slice 0（见 REPORT.md）· 编译/Clippy 0 警告

本轮目标：**不了解代码、不看日志、不用控制台的玩家，仅凭正常游戏界面
就能理解并完成整个 Slice 0 搬运循环。** 未新增任何核心玩法系统。

---

## UI implemented

新增/改造的正式玩家交互：

1. **框选批量搬运**：在地图上按住左键拖出青色矩形，松开即把框内所有
   未标记物品标记搬运——一次手势收走"一片"物品（满足"10~20 件物品
   不能要求 10~20 次精确点击"）。与 Haul All [H] 并存。
2. **悬停反馈**：光标指向船员/物品/货架时显示白色高亮环 +
   跟随光标的信息卡——船员（名字+当前状态）、物品（种类+是否标记/被谁
   领取/不可达）、货架（n/4、空位或 FULL，异常红色）。
3. **地图可读性**：六个房间的地面文字标注（CARGO HOLD / CREW QUARTERS /
   ORE BAY / PARTS ROOM / HOLD B / STORAGE）+ STORAGE 房间淡琥珀色
   地面高亮——不看任何说明就能找到"仓储在哪里"。
4. **任务状态五态可视化**（§十二）：
   未标记（无环）→ 已标记（琥珀菱形环）→ **已领取（环变为领取者颜色）** →
   携带中（物品消失，船员头顶出现物品小图）→ 已入库（货架 n/4 增长）。
   不可达时环变红。
5. **船员工作反馈**：空闲船员整只变暗（一眼分清谁在干活），
   工作中全彩 + 携带图标；底部状态条保留状态与原因文本。
6. **右键拖拽平移镜头** + 滚轮平滑缩放（插值趋近目标倍率），
   镜头始终被限制在船体附近，不会把船"拖丢"。
7. **调试工具折叠**：+Crate/+Ore/+Part 与 [X] 删除移入顶栏 Debug 开关
   之后的隐藏行，默认关闭；正式界面只剩玩家语义的按钮
   （速度 / Haul All / Cancel All / Debug）。
8. **实体 ID 清理**："被谁领取"从实体编号改为船员姓名（面板与 tooltip）；
   事件日志面板加 EVENT LOG 标题；帮助行更新为玩家操作说明。
9. 相机开局即见全船（沿用 Slice 0），本轮收紧了平移边界并平滑缩放。

## Playtest performed

两轮"正常玩家视角"自玩（不使用 debug 工具、不看控制台）：

**Round 1（初始状态）**——运行游戏截图检查：
- 全船与房间布局一眼可读；房间标注可见、STORAGE 琥珀高亮可辨；
- 顶栏按钮正确：Pause/1x/2x/4x（当前项高亮）、Haul All、Cancel All、
  **Debug**——确认 +Crate 等调试按钮已不在默认界面；
- 帮助行、EVENT LOG 标题、四个船员状态条、选中面板引导文案齐全；
- 货架 0/4 标签清晰。

**Round 2（搬运进行中，4× 速度下场景运行 18 秒后截图）**：
- 空闲船员明显变暗、1 名活跃船员全彩行走；
- 地面物品带琥珀菱形环，其中至少一个环为蓝色、一个为红色
  （= 已被对应颜色船员领取）；
- 一名船员头顶携带物品小图；
- 货架计数增长（1/4、2/4），顶栏 Storage: 4/24；
- 无文本重叠、无不可读 UI。

**逻辑自玩（自动化）**：`SLICE0_SCENARIO=A..F` 全部重跑通过
（正常搬运/满仓/不可达/竞争/失效/4× 稳定，结果与 Slice 0 基线一致）。

## Usability problems found and fixed

1. **ORE BAY 房间标注与地面物品/船员标签视觉重叠** →
   标注透明度 0.5→0.38、字号 13→12，作为"地面层"信息不与动态元素争抢。
2. **STORAGE 标注紧贴第一排货架** → 移到房间底部边缘，避开货架行。
3. **调试按钮直接暴露在正式顶栏** → Debug 折叠开关，默认隐藏。
4. **物品"claimed by crew #23"暴露实体编号** → 解析为船员姓名。
5. **截图中首次抓到旧进程窗口**（自玩流程问题）→ 截图前清理残留进程，
   确保 Round 2 抓到真实运行状态。
6. **右键拖拽平移与镜头钳制边距过宽** → 边距 300→220 倍率系数，
   缩放改为插值平滑。

## Remaining usability problems

（存在但不阻塞 0B）

1. 框选只能"标记"，没有对应的"框选取消标记"（取消靠 T/C 单件/全部）。
2. 悬停信息卡在窗口最底部可能贴近底栏（有简单钳制，未做避让布局）。
3. 一格宽门口的排队/穿插仍可见（避让仍是 Slice 0 的等待→绕行→穿行），
   4× 下偶尔出现短暂重叠——设计问题留待设计者判断（见下）。
4. 满仓后的"搬起→放下"失败循环有界但视觉噪音偏大。
5. UI 文案仍为英文（默认字体无 CJK 字形）。

## Design assumptions made

（为可玩性临时决定，非最终设计）

1. 框选拖动 = 标记搬运（不做反选/Shift 增减）；矩形由屏幕坐标换算世界坐标。
2. 已领取状态用"领取者颜色染环"表达，未加文字标签。
3. 空闲=变暗（45% 亮度）作为"谁没干活"的答案；未来情绪/疲劳系统
   出现后需要换表达。
4. 仓储仍是实体货架（Slice 0 假设不变），本轮只加房间高亮帮助识别。
5. 右键拖拽平移、不做边缘滚动；框选阈值 10px。
6. Debug 开关放进正式顶栏（而不是隐藏快捷键），便于设计者试玩时
   造场景——它是"开发者入口在玩家界面的一角"这一权衡。

## Technical decisions

1. **框选**：`BoxSelect` 资源记录屏幕坐标锚点/当前位置；左键释放时
   通过相机把两个角换算为世界坐标，发出 `Action::MarkArea { from, to }`；
   jobs 端遍历物品按世界坐标判矩形。事件化使批量标记可被自动测试
   （新增 2 个集成测试），UI 与模拟保持分离。
2. **拖动 vs 点击判别**：位移 ≤10px 视为点击（选择/取消选择），
   否则视为框选；起点在 UI 上时忽略整个手势。
3. **悬停**：`Hovered` 资源每帧由共享的 `pick_at_cursor`（船员>物品>货架）
   计算；渲染侧一个白色半透明环实体（Markers 池），UI 侧一个绝对定位
   tooltip 节点跟随光标（clamp 到窗口内），内容函数与选中面板复用
   （`task_label` / `item_status`）。
4. **已领取环颜色**：物品视觉同步系统查询 ReservedBy → 领取船员 tint，
   一次遍历完成，无新增组件。
5. **空闲变暗**：船员精灵 color = tint×0.45（Srgba 分量缩放），
   状态切换零成本。
6. **房间标注/高亮**：启动时按硬编码房间矩形生成 Text2d（地面层 z=0.02）
   与一个 `Sprite::from_color` 半透明色块（z=0.01），纯静态实体。
7. **Debug 折叠**：`DebugBarVisible` 资源 + UI 内部事件（复用 Action::
   ToggleDebug，由 ui 消费），`X` 删除键仅在 Debug 可见时生效，
   防止玩家误删。
8. **平滑缩放**：目标倍率 Local 缓存，每帧向目标插值（12/s），
   倍率范围 0.6–3.0。

## Automated tests

- **新增 2 个集成测试**（`tests/haul_logic.rs`，总数 17→19）：
  - `box_select_marks_only_items_inside_rect`：框内 2 件被标记、框外 1 件不受影响；
  - `box_select_is_idempotent_and_can_reselect_dropped`：取消标记后可再次框选。
- **回归**：9 个单元测试 + 8 个 Slice 0 集成测试全部通过；
  验收场景 A–F 自动驱动全部通过；`cargo clippy` 0 警告。

## Manual verification

（截图 + 自动化场景实测）

| 场景 | 验证 |
| --- | --- |
| 相机（平移/缩放/开局视野/边界） | 代码路径 + 两轮截图确认视野与布局 |
| 悬停环 + tooltip | 组件（环/文字路径）与选中面板共用逻辑，截图确认渲染管线 |
| 点击选择三类对象 | 集成测试覆盖选择状态机核心；截图确认选中渲染 |
| 框选批量标记 | 2 个集成测试 + 场景 A（MarkAll 等价路径） |
| 空闲/工作/携带区分 | Round 2 截图确认（变暗、携带图标、环色） |
| 仓储满 | 场景 B：FULL 红显、放下+取消标记、无往返 |
| 不可达 | 场景 C：红环 + 日志 + 不阻塞 |
| 多人竞争 | 场景 D：四人四目标、环色互异 |
| 目标失效 | 场景 E：安全取消并再领取 |
| 4× 高速 | 场景 F + Round 2 在 4× 下截图可读 |
| 没有工作 | 空闲原因在状态条/面板/tooltip 三处可见 |

## Art assets added or changed

无新增生成资产（Slice 0 的 9 张已满足可读性优先级）。
本轮新增视觉元素全部为程序化：房间文字标注、STORAGE 琥珀色块、
悬停环（复用 ring.png 调白色）、框选矩形（UI border）。

## Recommended design questions

（试玩后再决定，不阻塞实现）

1. **框选交互**：现在的"拖=标记"是否保留？要不要右键框选=取消标记，
   或专用"搬运工具"模式？
2. **已领取=领取者颜色**是否足够直觉？是否需要在物品上加小徽章/名字？
3. **空闲=变暗**在未来有士气/睡眠等状态后会被占用，空闲的最终视觉语言是什么？
4. **Debug 入口**：放在顶栏角落 vs 完全隐藏（如启动参数），取决于设计者
   想让测试场景多容易被造出来。
5. 仓储满时的"放下重试噪音"：是否需要"满仓时暂停领取已放下物品"的
   更温和表达（影响玩家对失败的理解节奏）？
6. 一格门拥堵与穿行重叠在 4× 下仍可见——是否进入下一轮避让
   （侧身让路/占用预约）的优先级？

---

**停止条件确认**：正常玩家可仅凭画面完成——浏览飞船、识别船员/物品/
仓储、选择对象、框选批量下达搬运、看懂执行/空闲/失败原因、查看仓储
容量、控制时间与镜头，全程无需 debug 工具、日志或文档。
**Slice 0B 到此停止，未开始 Slice 1 或任何新玩法系统。**
