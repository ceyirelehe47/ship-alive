# Playable Slice 0 — Ship Alive 交付报告

日期：2026-08-16 · 引擎：Rust + Bevy 0.16.1 · 平台：Windows（窗口 1440×860）

---

## Implementation completed

实际已经可以玩的功能（`cargo run` 启动，操作见 `PLAYTEST.md`）：

- **Starter Ship**：36×19 固定布局。四个房间（货舱 / 矿舱 / 船员舱 / 零件间）+
  仓储舱（6 货架格 × 4 格位 = 容量 24）、2 格宽主走廊、多处 1 格宽门、
  以及一个被舱壁完全封闭的隔舱（内置 1 个不可达货箱，用于场景 C）。
- **4 名船员**（Ava/Rex/Mio/Zed，红/蓝/绿/紫）：A* 寻路、逐格移动、
  软避让（等待 → 绕行 → 超时穿行），不会穿墙、不会卡死。
- **地面物品**：3 种（货箱 / 矿石 / 机械件），开局 15 件，分布在三个房间；
  支持同格堆叠（带视觉错位）。
- **仓储**：实体货架格（可走上去存放），每格容量 4、按种类计数、
  格子上方显示 `n/4`，顶栏显示总量并在满仓时红色 `FULL`。
- **搬运工作系统**：标记意图（单件 `T` / 全部 `H` / 取消 `C`，均有按钮）→
  空闲船员自动认领最近的可达目标 → 走到物品 → 拾取（0.3s）→ 携带 →
  走到货架 → 入库（0.25s）→ 立即寻找下一项工作。
- **Reservation 与失败恢复**：领取互斥；目标消失 / 中途取消标记 / 途中满仓 /
  路径不可达 / 货架消失等路径全部安全收敛，无永久卡死、无脏预约
  （详见 Tests 中的对应用例）。
- **时间控制**：暂停 / 1× / 2× / 4×（键盘 + 按钮），基于 Bevy 虚拟时钟，
  暂停时任务系统完全停摆。
- **最低限度 UI**：顶栏（标题、速度、已标记数、仓储余量、空闲人数、时钟）、
  底部每名船员实时状态条（含已送达数）、选中详情面板（船员 / 物品 / 货架三类）、
  事件日志（领取/入库/失败/取消，失败红色）、选中船员的青色路径点 +
  绿色目标标记、被标记物品的琥珀色菱形环（不可达时变红）。
- **美术**：9 张资产全部由 Codex image generation 生成（统一深色舰内风格、
  洋红抠像底），经 `prep_art` 工具去背/裁切/补方/缩放为 256px 透明 PNG 接入。
- **调试工具**：`+Crate/+Ore/+Part` 随机生成、`X` 删除选中物品（制造目标失效）、
  `SLICE0_SCENARIO=A..F` 自动验收驱动器、`SLICE0_TRACE=1` 移动轨迹打印。

## Tests

**自动测试（`cargo test`，全部通过，17 个）**

- 单元测试 ×9：
  - 地图解析（行宽/生成物计数）、可走性、世界坐标往返；
  - A*：直线、绕墙、不可达返回 None、动态阻挡目标、零长度路径；
  - 货架容量：加满、超量拒绝、计数与标签。
- 集成测试 ×8（`tests/haul_logic.rs`，裸 `bevy_ecs` World + Schedule，
  手动推进虚拟时钟，无需窗口）：
  - 两船员同帧扫描只允许一人认领（领取互斥，场景 D 内核）；
  - 完整流程：走到→拾取→搬运→入库→物品 despawn、货架+1、船员回到 Idle；
  - 取消标记：任务终止 + Reservation 释放；
  - 目标实体被删除：安全取消、无 panic、无残留；
  - 满仓时不领取（IdleCause::NoStorageSpace）；
  - 不可达目标：写入 15s 冷却、无预约、船员 IdleCause::AllUnreachable（场景 C 内核）；
  - 途中满仓：后到者把物品**放下并取消标记**，系统随后保持稳定不重抢；
  - 携带状态：CarriedBy 归属、相位进入 ToStorage。

**自动验收场景（`SLICE0_SCENARIO=A..F cargo run`，全部通过）**

- A 正常搬运：15 件中 14 件入库（剩余 1 件为按设计不可达的封舱货箱），
  四人分头领取不同目标，无卡死。
- B 仓储满：超量生成物品后 24/24 满、空闲原因显示 `no free storage space`、
  在途者放下物品并取消标记，无无限往返。
- C 不可达：封舱货箱被标记后识别 `unreachable`，15s 冷却重试，其余工作不受影响。
- D 多人竞争：四人同帧醒来，认领四个**不同**物品，资源不重复计入。
- E 目标失效：途中删除被认领目标，任务安全终止并立即领取新工作。
- F 时间加速：4× 下大量任务，行为稳定、无任务复制/丢失/死锁。

**编译状态**：`cargo build` / `cargo clippy` 0 error / 0 warning；
dev profile（opt-level 1 + 依赖 opt 3）下运行流畅。

**仍未覆盖（需手动）**：视觉布局与可读性的主观判断、镜头操作手感、
点击拾取的像素级精度 —— 手动步骤已在 `PLAYTEST.md` 给出。
（开发期间曾用 PrintWindow 截图 + 像素统计验证过渲染正确性。）

## Design assumptions made

（为继续开发而临时采用的玩法假设，均非最终设计）

1. **仓储形态选择了实体货架格**（1 格 = 1 货架，可走上去，容量 4，混装种类）——
   这正是本轮要同"仓储区域"方案对比的样本，报告末尾有试玩观察建议。
2. 船员一次只能搬 1 件物品。
3. 允许地面物品同格堆叠（视觉上小幅错开）。
4. 玩家只表达搬运意图（标记），不直接命令单个船员；系统按"最近优先"自动分配。
5. 认领时只要求"存在任一有空位的货架"，逐货架可达性在交付时校验。
6. 交付失败被放下的物品会**取消标记**，防止满仓时无限重抢。
7. 不可达目标按 15 秒冷却重试。
8. 拾取/入库各有一个短暂停顿（0.3s / 0.25s）用于可读性。
9. 生成按钮产出的是**已标记**物品（调试便利）。
10. 船员受阻超过一定时间后可以互相穿过（见 Temporary behaviors）。
11. UI 文案仅英文（默认字体无 CJK 字形）。

## Temporary behaviors

- **穿行避让**：船员被挡 0.6s 后尝试绕路，累计受阻 1.5s 后直接穿过对方
  （短暂重叠）。没有真正的侧身让路。
- 货架计数是浮在货架上方的小字文本，不是图形化格位。
- 速度档位为 1×/2×/4×；Space 在暂停↔1× 间切换（不记忆原速度）。
- 随机生成用 xorshift + 墙钟，未接 RNG 框架。
- 选中目标被入库后选中框静默消失（无提示）。
- 船员姓名/颜色为硬编码名册。

## Design problems discovered

（只记录，未擅自固化为新规则）

1. **1 格宽门 + "最近货架"路由 = 严重的漏斗拥堵**：四个船员同时向仓储门汇聚，
   开发中曾观测到门口活锁（详见 Technical decisions #4）。穿行机制掩盖了死锁，
   但排队/穿插仍是常态——这正是文档问题 7（一格走廊是否允许）的直接证据。
2. **最近优先认领缺乏负载均衡**：所有人冲向同一片区域，路径交叉明显
   （文档问题 4：会不会频繁互相抢任务/撞车）。
3. **Haul All + 满仓**会产生一段"搬起→放下"的失败循环期（有界但视觉噪音大），
   可能需要"满仓时暂不领取已放下物品"的更优雅表达。
4. **距离对效率的影响极其直观**：货舱→仓储一趟约 25–30 游戏秒，
   矿舱→仓储约一半（文档问题 9 的现成答案素材）；同时也暴露
   "单件搬运 + 长距离"吞吐量低的问题（一次搬多件会显著改变手感）。
5. 货架可走上去意味着船员会**站在**货架上作业；俯视角下无碍，
   但未来若有货架朝向/背面规则会冲突。
6. 标记环在小缩放下不太醒目，满地物品时有一定视觉噪音（文档问题 5）。

## Technical decisions

1. **地图**：`ShipMap` 资源持有稠密 `Vec<Tile>`（Wall/Floor），布局用字符画
   `MAP_LAYOUT` 手写并断言行宽。动态物（船员/物品/货架）才是 ECS 实体，
   通过 `TilePos` 引用网格。未来第二张地图 = 另一个 `ShipMap` + 跨图连接点。
2. **寻路**：4 向 A*（曼哈顿启发式），`blocked` 谓词参数化，
   同时用于认领可达性、常规路由和拥堵绕行。
3. **任务系统**：没有独立任务板。任务=派生状态：
   `MarkedForHaul`（意图）+ `ReservedBy`（占用）+ 船员 `CrewTask::Haul`（执行）。
   任何结束路径都经由统一的 `end_haul` 释放资源，从结构上杜绝脏预约。
   认领在单系统内串行完成（天然原子），系统内再用本地 claim 集合
   处理同帧 Commands 延迟导致的可见性窗口。
4. **移动与避让**：逐格推进 + `progress` 插值渲染。避让三段式：
   等待（0.6s）→ 以他人站位为墙重寻路 → 累计受阻 1.5s 穿行。
   关键教训：受阻计数用**衰减**而非清零——否则门口的振荡阻塞永远到不了
   穿行阈值，形成 0.1 格/秒的活锁（场景实测发现并修复）。
5. **视觉层**：逻辑实体不持有任何渲染组件；每个逻辑实体对应若干
   `Visual { target, role }` 视觉实体，孤儿清理系统每帧回收——
   物品入库 despawn 后精灵必然消失，不依赖 despawn 递归语义。
6. **时间**：`Time<Virtual>` 相对速度实现暂停/倍速；全部模拟系统只读虚拟时钟，
   因此测试可以手动 `advance_by` 逐帧驱动。
7. **Bevy 结构**：7 个小插件（setup/render/time/input/jobs/movement/ui/autotest），
   帧内四阶段 `Input → Jobs → Move → Sync` 链式集合；UI 与模拟仅通过
   `Action` 事件与只读查询交互。
8. **美术管线**：codex imagegen（洋红纯色底）→ `prep_art`（边缘泛洪抠像、
   裁切、补方、256px Lanczos）→ `assets/art/`。运行时资产缺失自动退化为
   程序化色块，仓库任何状态都能跑。
9. **自动验收**：`autotest.rs` 用环境变量注入脚本化玩家操作并输出
   `SCENARIO_RESULT` 摘要 + 日志尾部，作为场景 A–F 的可重复证据。

## Recommended playtest checks

第一轮实机试玩最值得重点观察（对应设计文档第十八节）：

1. **门口拥堵**：盯住仓储舱两个 1 格门（下方中部/右部）。排队穿插是否频繁到
   影响理解？穿行重叠是否可接受？（问题 7 / 13）
2. **自动分配是否够用**：只按 H 看完一整轮。你是否产生过"想直接点某人去搬某物"
   的冲动？冲动出现在什么情境？（问题 1 / 2 / 3）
3. **距离→效率体感**：对比矿舱（近）与货舱（远）的入库速度差和船员状态条上
   delivered 数字。（问题 9）
4. **仓储形态**：实体货架（可走上去、有格位计数）vs 你想象中的 RimWorld 式
   仓储区域，管理体验哪个更顺？（问题 6）
5. **满仓表达**：把仓储塞满，看红色 FULL、空闲原因、放下物品的事件流是否
   足够解释"为什么没人在干活"。
6. **视觉噪音**：满地物品 + 标记环在默认缩放下是否混乱？（问题 5）
7. **4× 可读性**：最高速下你是否还能说清每人在干什么？（问题 10）
8. **地图尺寸**：36×19 是否合适，步行是否无聊或太挤？（问题 8）

## Recommended next slice

（仅建议，未实现）

1. **让避让更像"让"**：侧身让路/占用预约，替代穿行——本轮已证明纯等待会活锁。
2. **搬运分配策略实验**：认领时加入负载/区域均衡，观察交叉路径是否减少。
3. **仓储对比原型**：加一种"仓储区域"实现，同图 A/B 试玩，回答货架 vs 区域。
4. **一次多件搬运**（或手推车）：显著改变吞吐与手感，影响地图尺寸结论。
5. **搬运优先级 / 暂停某类物品**的轻量表达。

---

## 停止条件核对

| 条件 | 状态 |
| --- | --- |
| 4 名船员稳定移动 | ✅（含拥堵自恢复） |
| 玩家可产生搬运需求 | ✅（标记/全标记/取消） |
| 船员自动领取 | ✅（最近优先，互斥） |
| 走到→携带→运送→入库全链路 | ✅（空间中真实发生） |
| 多船员不无意义抢同一目标 | ✅（场景 D + 测试） |
| 常见失败不永久卡任务 | ✅（场景 B/C/E + 测试） |
| 时间加速下稳定 | ✅（场景 F） |
| UI 足够理解行为 | ✅（状态条/面板/日志/路径标记） |
| 最低限度可读美术 | ✅（9 张生成资产） |
| 验收场景实际测试 | ✅（A–F 自动实测） |
| 正常编译运行 | ✅（0 警告） |
| 交付报告 | ✅ 本文档 |

**Slice 0 到此停止，等待设计者试玩与下一轮设计反馈。**
