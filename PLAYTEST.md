# Ship Alive — 试玩指南（Slice 0 + 0B）

## 启动

```bash
cargo run            # 开发模式即可流畅运行
cargo run --release  # 更流畅
```

窗口 1440×860。整艘 Starter Ship（36×19 格）开局就在视野内，
地板上有房间名标注（CARGO HOLD / CREW QUARTERS / ORE BAY / PARTS ROOM / HOLD B / STORAGE），
**STORAGE 房间带淡琥珀色地面高亮**。

## 基础操作（全部可通过鼠标完成）

| 操作 | 效果 |
| --- | --- |
| 左键点击船员 / 物品 / 货架 | 选中，左下面板显示详情 |
| 左键点击空地 | 取消选中 |
| **左键在地图上拖动** | 框选：框内所有物品标记搬运（青色矩形预览） |
| 鼠标悬停 | 白色高亮环 + 跟随光标的信息卡（名称/状态/领取者/容量） |
| 右键拖动 / `WASD` / 方向键 | 平移镜头 |
| 滚轮 | 平滑缩放 |
| `T` | 标记/取消选中物品的搬运 |
| `H` | 全部标记搬运（顶栏也有 Haul All 按钮） |
| `C` | 取消全部搬运（携带中的物品会被放下） |
| `Space` / `1` / `2` / `3` | 暂停 / 1× / 2× / 4×（顶栏按钮等效，当前速度高亮） |
| `Esc` | 取消选中 |

## 怎么看懂正在发生什么

- **物品状态（地图直接可见）**：
  - 无标记 = 躺在地上；
  - **琥珀色菱形环** = 已标记待搬运；
  - 环变成**某船员的颜色** = 已被该船员领取（红=Ava 蓝=Rex 绿=Mio 紫=Zed）；
  - 物品消失 + 船员头顶出现小图标 = 正被携带；
  - 环变红 = 该目标当前不可达（15 秒后自动重试）。
- **船员状态**：头顶名字；**空闲的船员明显变暗**；携带时头顶有物品小图。
  底部状态条实时显示每人状态与原因（Idle — no free storage space 等）和已送达数。
- **仓储**：每个货架显示 `n/4`；顶栏显示总量 `Storage: 8/24`，**满仓变红 FULL**。
- **选中船员**时：青色路径点 + 黄色选中框 + 绿色目标标记。
- **事件日志**（右下，带 EVENT LOG 标题）：领取/入库/失败/取消全程记录，失败为红色。

## 验收场景步骤（正常 UI 即可完成）

### A 正常搬运
拖框选中散落物品（或点 Haul All）→ 观察四人分头领取、搬运、入库，
最终除密封舱内 1 件外全部入库（容量 24 > 初始 15）。

### B 仓储已满
点顶栏 **Debug** 按钮展开调试工具 → 连点 `+Crate` 约 15 次超出容量。
仓储满后：顶栏红色 FULL、空闲船员显示 `no free storage space`、
在途物品被放下并取消标记（红色日志）。

### C 不可达物品
框选/全标后：密封舱内货箱（地图中央被墙围死的小格子）标记环变红，
日志出现 `unreachable`，其余工作不受影响。

### D 多人竞争
开局立刻 Haul All：四人认领四个不同目标（看日志与标记环颜色互不相同）。

### E 目标失效
选中某个**正被前往搬运**的物品 → Debug 展开后按 `X` 删除：
该船员任务安全取消，立刻领取新工作。

### F 时间加速
`H` + `4×`，可再 Debug 生成物品加压：行为稳定可读。

## 开发者工具

- 顶栏 **Debug** 开关（默认折叠）：+Crate / +Ore / +Part 生成、`X` 删除选中物品。
- `SLICE0_SCENARIO=A..F cargo run`：自动验收场景（输出结果摘要后自动退出）。
- `SLICE0_TRACE=1 SLICE0_SCENARIO=A cargo run`：每 2 秒打印船员移动轨迹。
- `cargo test`：19 个单元/集成测试。
