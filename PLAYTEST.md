# Ship Alive — Playable Slice 0 试玩指南

## 启动

```bash
cargo run            # 开发模式即可流畅运行
cargo run --release  # 更流畅
```

窗口 1440×860，地图为固定布局的 Starter Ship（36×19 格）。

## 你会看到什么

- **四个房间 + 双向走廊**：左上货舱（货箱）、右上矿舱（矿石）、左下零件间（机械件）、
  右下**仓储舱**（2×3 共 6 个货架格，每格 4 格位，总容量 24）。
- 中间上部小房间是**船员舱**，4 名船员（Ava/Rex/Mio/Zed，红/蓝/绿/紫）开局站在这里。
- 地图中央偏下有一个**被舱壁完全封闭的小格子**，里面锁着 1 个货箱
  （用于验证"不可达目标"场景 C）。

## 基本操作

| 操作 | 效果 |
| --- | --- |
| 左键点击船员 / 物品 / 货架 | 选中并查看详情（左下面板） |
| 左键点击空地 | 取消选中 |
| `T` | 标记/取消选中物品的搬运意图 |
| `H` | 全部标记搬运（Haul All） |
| `C` | 取消全部搬运（Cancel All，携带中的物品会被放下） |
| `X` | 删除选中的物品实体（调试用，用于制造"目标失效"） |
| `Space` / `1` / `2` / `3` | 暂停 / 1× / 2× / 4× |
| `WASD` / 方向键 | 平移镜头 |
| 滚轮 | 缩放 |

顶栏按钮与快捷键等效，另有 `+Crate / +Ore / +Part` 调试按钮
（在左上货舱随机空格生成一个**已标记**的物品）。

## 关键 UI

- **顶栏**：速度按钮（当前速度高亮）、已标记数、仓储 `已存/总容量`（满时红色 FULL）、
  空闲人数、游戏时钟。
- **船员状态条**（底部）：每名船员实时状态 + 已送达数，如
  `[>] Mio: Carrying Asteroid Ore to rack at (28,11)  (delivered 3)`。
- **左下选中面板**：船员（当前阶段、目标坐标、剩余步数）、物品（是否被标记/认领、
  不可达倒计时）、货架（分类存量、空位）。
- **右下事件日志**：领取/入库/失败/取消的完整记录，失败为红色。
- 选中船员时，地图上有**青色路径点**、黄色选中框和**绿色目标标记**。

## 验收场景手动步骤

### 场景 A — 正常搬运
1. 开局按 `H`，观察 4 名船员分别认领不同物品。
2. 等待/切 4×：所有可搬运物品最终入库（初始 15 件物品、容量 24，密封舱内 1 件除外）。
3. 期望：无人卡死，日志可见完整"领取→入库"链。

### 场景 B — 仓储已满
1. 连续点击 `+Crate` 约 15 次（超出容量）。
2. 等仓储满：顶栏变红 `FULL`，空闲船员显示 `no free storage space`。
3. 正在途中的最后几件会被**放下**并取消标记，日志出现
   `dropped the item … no free storage space`。
4. 期望：没有船员在物品和仓库之间无限往返。

### 场景 C — 不可达目标
1. 按 `H`：密封舱内的货箱被标记。
2. 日志出现 `Cargo Crate at (16,13) is unreachable`，其标记环变红，15 秒后重试一次。
3. 期望：船员不会反复冲击该目标，其它工作正常进行。

### 场景 D — 多人竞争
1. 开局立刻按 `H`：4 名船员同一帧醒来抢任务。
2. 期望：日志显示他们认领的是**四个不同物品**（领取互斥）。

### 场景 E — 目标失效
1. 按 `H` 后，点击某个**正被前往搬运**的物品选中它。
2. 按 `X` 删除：该船员任务取消（`target vanished`），立刻回到可工作状态。
3. 期望：无永久 Reservation 残留（该物品实体已消失）。

### 场景 F — 时间加速
1. `H` + `3`（4×），并再点几次 `+Ore` 增加负载。
2. 期望：行为稳定，无任务复制/丢失/死锁。

## 自动化验收（无需人工）

```bash
SLICE0_SCENARIO=A cargo run   # B/C/D/E/F 同理，输出 SCENARIO_RESULT 与日志尾部
cargo test                    # 单元 + 集成测试（任务领取/预约/取消/满仓/不可达/释放）
SLICE0_TRACE=1 SLICE0_SCENARIO=A cargo run   # 每 2 秒打印船员移动轨迹（调试）
```
